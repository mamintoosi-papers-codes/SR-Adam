# راهنمای مایگریشن نتایج قدیمی 🔄

## مشکل

نتایج قدیمی شما در این فرمت ذخیره شده‌اند:
```
results/CIFAR10/simplecnn/noise_0.0/Adam/run_1_metrics.csv
runs/CIFAR10/simplecnn/noise_0.0/Adam/run_1_best.pt
```

اما کد جدید انتظار این فرمت را دارد:
```
results/CIFAR10/simplecnn/noise_0.0/Adam/batch_size_128/run_1_metrics.csv
runs/CIFAR10/simplecnn/noise_0.0/Adam/batch_size_128/run_1_best.pt
```

## راه‌حل

یک اسکریپت خودکار برای شما آماده کردم که تمام نتایج قدیمی را به فرمت جدید منتقل می‌کند.

---

## مراحل استفاده

### مرحله 1️⃣: اجرای اسکریپت

```bash
python migrate_results_format.py
```

### مرحله 2️⃣: بررسی پیش‌نمایش

اسکریپت ابتدا **Dry Run** (حالت نمایشی) اجرا می‌شود و به شما نشان می‌دهد چه فایل‌هایی را جابجا خواهد کرد:

```
================================================================================
Migrating: results
Batch size: 128
Dry run: True
================================================================================

Found 15 old-format files:

Old: CIFAR10/simplecnn/noise_0.0/Adam/run_1_metrics.csv
New: CIFAR10/simplecnn/noise_0.0/Adam/batch_size_128/run_1_metrics.csv
[DRY RUN] Would move

Old: CIFAR10/simplecnn/noise_0.0/Adam/run_2_metrics.csv
New: CIFAR10/simplecnn/noise_0.0/Adam/batch_size_128/run_2_metrics.csv
[DRY RUN] Would move

...
```

### مرحله 4️⃣: اجرای واقعی

اسکریپت شروع به جابجایی فایل‌ها می‌کند:

```
[3] Performing actual migration...

Old: CIFAR10/simplecnn/noise_0.0/Adam/run_1_metrics.csv
New: CIFAR10/simplecnn/noise_0.0/Adam/batch_size_128/run_1_metrics.csv
✓ Moved successfully
```

### مرحله 5️⃣: پاکسازی پوشه‌های خالی

بعد از جابجایی، پوشه‌های خالی پاک می‌شوند:

```
[4] Cleaning up empty directories...
Removing empty directory: CIFAR10/simplecnn/noise_0.0/Adam
```

---

## خروجی نهایی

```
================================================================================
MIGRATION SUMMARY
================================================================================

Results directory:
  Files moved: 12
  Errors: 0
  Empty dirs cleaned: 4

Runs directory:
  Files moved: 10
  Errors: 0
  Empty dirs cleaned: 4

Total:
  Files moved: 22
  Errors: 0
  Empty dirs cleaned: 8

✓ Migration completed successfully!
================================================================================
```

---

## ساختار قبل و بعد

### قبل از مایگریشن:
```
results/
  CIFAR10/
    simplecnn/
      noise_0.0/
        Adam/
          run_1_metrics.csv        ⬅️ مستقیم در پوشه Adam
          run_2_metrics.csv
          aggregated_summary.json
```

### بعد از مایگریشن:
```
results/
  CIFAR10/
    simplecnn/
      noise_0.0/
        Adam/
          batch_size_128/          ⬅️ یک پوشه جدید اضافه شد
            run_1_metrics.csv
            run_2_metrics.csv
            aggregated_summary.json
├── runs/
│   └── CIFAR10/
│       └── simplecnn/
│           └── noise_0.0/
│               └── Adam/
│                   └── batch_size_128/          ← پوشه جدید
│                       ├── run_1_best.pt
│                       ├── run_1_final.pt
│                       ├── run_2_best.pt
```

---

## نکات مهم ⚠️

### 1. پشتیبان‌گیری (اختیاری)
اگر می‌خواهید محتاطانه عمل کنید، قبل از اجرا یک نسخه پشتیبان بگیرید:

```bash
# پشتیبان‌گیری از results
xcopy results results_backup /E /I /H /Y

# پشتیبان‌گیری از runs  
xcopy runs runs_backup /E /I /H /Y
```

### 2. اجرای مجدد
اگر اسکریپت را دوباره اجرا کنید، پیغام زیر را می‌بینید:

```
No old-format files found in results
✓ No old-format files found. Your results are already in the new format!
```

### 3. حجم فایل‌ها
- اسکریپت فایل‌ها را **جابجا می‌کند** (move) نه کپی
- فضای اضافی اشغال نمی‌شود
- سرعت بالا (حتی برای هزاران فایل)

### 4. سازگاری با کد جدید
بعد از مایگریشن، کد جدید (`main.py`) به صورت خودکار نتایج را در پوشه‌های `batch_size_128/` پیدا می‌کند و استفاده مجدد می‌کند.

---

## عیب‌یابی

### مشکل: "No old-format files found"
**دلیل:** نتایج شما قبلاً به فرمت جدید هستند یا `results/` و `runs/` خالی هستند.

**راه‌حل:** نیازی به کار اضافی نیست.

---

### مشکل: "Permission denied"
**دلیل:** فایل‌ها باز هستند یا دسترسی ندارید.

**راه‌حل:**
1. تمام برنامه‌هایی که فایل‌ها را باز کرده‌اند ببندید (Excel, VS Code, ...)
2. اسکریپت را با دسترسی Administrator اجرا کنید

---

### مشکل: برخی فایل‌ها جابجا نشدند
**دلیل:** ممکن است فایل‌ها قبلاً در پوشه‌های `batch_size_*` باشند.

**راه‌حل:** اسکریپت فقط فایل‌هایی در فرمت قدیمی را جابجا می‌کند. فایل‌های موجود در `batch_size_*` دست نخورده باقی می‌مانند.

---

## مثال کامل

```bash
(pth) C:\SR-Adam> python migrate_results_format.py

================================================================================
SR-Adam Results Format Migration Tool
Old format: {base}/{dataset}/{model}/noise_{noise}/{optimizer}/run_*.csv
New format: {base}/{dataset}/{model}/noise_{noise}/{optimizer}/batch_size_128/run_*.csv
================================================================================

[1] Analyzing existing files...
    Results directory: 12 old-format files found
    Runs directory: 10 old-format files found

[2] Performing dry run (preview mode)...
... (پیش‌نمایش جابجایی‌ها) ...

Proceed with migration of 22 files? (yes/no): yes

[3] Performing actual migration...
✓ Moved successfully (22 files)

[4] Cleaning up empty directories...
Removed 8 empty directories

================================================================================
MIGRATION SUMMARY
================================================================================
Total:
  Files moved: 22
  Errors: 0
  Empty dirs cleaned: 8

✓ Migration completed successfully!
================================================================================
```

---

## سوالات متداول

### آیا فایل‌های من پاک می‌شوند؟
❌ خیر! فایل‌ها فقط **جابجا** می‌شوند (move)، نه پاک.

### آیا محتوای فایل‌ها تغییر می‌کند؟
❌ خیر! فقط **مسیر** فایل‌ها تغییر می‌کند، محتوا دست نخورده باقی می‌ماند.

### چند وقت طول می‌کشد؟
⚡ خیلی سریع! حتی برای صدها فایل، کمتر از یک دقیقه.

### اگر اشتباهی "no" بزنم چی می‌شه؟
✅ هیچ اتفاقی نمی‌افتد. می‌تونید دوباره اجرا کنید.

### آیا می‌تونم برگردونم؟
✅ بله، اگر لازم باشد می‌تونید با دستور زیر برگردونید:
```bash
# مثال: برگرداندن فایل‌های Adam
move results\CIFAR10\simplecnn\noise_0.0\Adam\batch_size_128\*.* results\CIFAR10\simplecnn\noise_0.0\Adam\
```

---

**✅ حالا آماده‌اید! اسکریپت را اجرا کنید:**

```bash
python migrate_results_format.py
```

